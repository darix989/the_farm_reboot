# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
gemini_gem_export

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Ludo.ai generated art awaiting review (see scripts/generate-emotion-sprites.mjs)
.ludo-review/

# API keys for the asset pipeline (LUDO_API_KEY) — also covered by the `*.local` rule
# above; named explicitly so it is obvious this file must never be committed.
.env.local
